# No focal runtime defaults are authoritative. Populate these from the run manifest;
# blank values remain invalid.
LLM_BASE_URL=
LLM_MODEL=
LLM_API_KEY=
LLM_TIMEOUT_SECONDS=

# Tokens are optional only for separately documented local components that require them.
HUGGING_FACE_TOKEN=
