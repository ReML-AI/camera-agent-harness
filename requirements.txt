# ================================================================================
# Clinical Simulation Feedback System - Unified Dependencies
# ================================================================================

# GPU framework and WhisperX
# --------------------------------------------------------------------------------
# Install with scripts/setup/install_env.sh.  Torch 2.5.1's published metadata names
# a cuDNN wheel which is no longer served, while the PyPI WhisperX 3.7.4 metadata
# incorrectly requires Torch 2.8 for this study's working stack.  Those four packages
# therefore require ordered --no-deps installs and are intentionally not listed as
# ordinary resolver inputs here.  Their exact versions remain pinned in
# constraints-gpu.txt and are asserted by the installer.

# Person Detection & Tracking
# --------------------------------------------------------------------------------
# ultralytics==8.4.8 is installed by install_env.sh without its GUI OpenCV dependency;
# opencv-python-headless below provides the cv2 runtime on compute nodes.
filterpy>=1.4.5              # Kalman filter for tracking
scipy>=1.11.0                # Scientific computing
lap>=0.4.0                   # Linear assignment problem solver
torchreid>=0.2.5             # Person re-identification
onnxruntime==1.23.2          # Execute the staged OSNet ONNX graph
onnx>=1.16.0                 # Export and validate operator-supplied OSNet graphs

# Speech Recognition & Transcription
# --------------------------------------------------------------------------------
# whisperx==3.7.4 is installed by scripts/setup/install_env.sh after its dependencies.
ctranslate2==4.6.3
faster-whisper==1.2.1
av==15.1.0
openai-whisper                              # Speech-to-text

# OCR & Document Processing
# --------------------------------------------------------------------------------
easyocr==1.7.2               # OCR version used to measure recorded model hashes
paddleocr                    # Alternative OCR
paddlepaddle                 # PaddleOCR backend

# Video & Image Processing
# --------------------------------------------------------------------------------
opencv-python-headless==5.0.0.93  # Headless: compute nodes have no libGL
moviepy                      # Video manipulation
Pillow>=10.0.0              # Image processing

# NLP & Language Models
# --------------------------------------------------------------------------------
transformers==4.57.6         # Last measured version compatible with this stack
huggingface-hub==0.36.0      # pyannote 3.4 still passes use_auth_token
sentence-transformers        # Sentence embeddings

# Machine Learning & Classification
# --------------------------------------------------------------------------------
scikit-learn>=1.3.0         # Role classification
xgboost>=2.0.0              # Gradient boosting

# Web Framework (Backend)
# --------------------------------------------------------------------------------
fastapi==0.104.1            # REST API framework
uvicorn[standard]==0.24.0   # ASGI server
sqlalchemy==2.0.23          # Database ORM
python-multipart==0.0.6     # File upload support
pydantic==2.5.0             # Data validation

# Auxiliary labeling UI
# --------------------------------------------------------------------------------
streamlit>=1.30.0           # Currently used for labeling/annotation

# LLM Client (for local Qwen 7B via the resolved OpenAI-compatible Ollama endpoint)
# --------------------------------------------------------------------------------
openai>=1.0.0

# Data Science & Visualization
# --------------------------------------------------------------------------------
numpy==2.0.2                 # Satisfies WhisperX 3.7.4's Python 3.10 metadata
pandas==2.2.3
sympy==1.13.1                # Required exactly by Torch 2.5.1
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0                # Progress bars

# Configuration & Utilities
# --------------------------------------------------------------------------------
python-dotenv               # Environment variables
pyyaml                      # YAML configuration
pytest==8.3.5               # Required by scripts/setup/preflight.py

# Speaker diarization stack, pinned to the versions the staged pyannote 3.1 models
# were resolved against. Do not float these: 4.x requires a different model set.
pyannote.audio==3.4.0
speechbrain==1.0.3

# Light-ASD imports this at module scope (third_party/Light-ASD/Columbia_test.py);
# it is not declared by the upstream repo, so stage 4 fails without it.
python_speech_features==0.6
scenedetect==0.6.4
