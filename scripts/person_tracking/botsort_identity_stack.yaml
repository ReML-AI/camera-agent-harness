# Pinned for ultralytics 8.4.8. These are UNVALIDATED DEFAULTS PENDING
# CALIBRATION, not empirically tuned values. Appearance ReID is deliberately
# disabled inside BoT-SORT; the separately pinned OSNet stage performs ReID.
tracker_type: botsort
track_high_thresh: 0.25
track_low_thresh: 0.10
new_track_thresh: 0.25
track_buffer: 30
match_thresh: 0.80
fuse_score: true
gmc_method: sparseOptFlow
proximity_thresh: 0.50
appearance_thresh: 0.80
with_reid: false
model: auto
